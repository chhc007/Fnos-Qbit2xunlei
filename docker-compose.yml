services:
  fnos-qbit2xunlei:
    build: .
    container_name: fnosqbit2xunlei
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config:/app/config
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
